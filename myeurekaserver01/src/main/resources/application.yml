server:
  port: 7001
eureka:
  client:
    service-url:
      defaultZone: http://localhost:7001/eureka/
    register-with-eureka: false
    fetch-registry: false
security:
  basic:
    enabled: true
  user:
    name: root
    password: ok